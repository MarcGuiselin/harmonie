# This file is auto-generated to compile mod {{file_name}}
#
# Modloader Version: {{modloader_version}}
# Dev Mode: {{dev_mode}}
# Build mode: Local

[package]
name = "{{package_name}}"
version = "0.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
path = "{{source_file}}"

[dependencies]
{{namespace}} = { package = "bevy_harmonize_api", path = "../api" }

[features]
wasm_runtime = ["{{namespace}}/wasm_runtime"]
generate_manifest = ["{{namespace}}/generate_manifest"]
