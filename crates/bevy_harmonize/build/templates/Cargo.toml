# This file is auto-generated to compile the mods in a shared workspace
#
# Modloader Version: {{modloader_version}}
# Dev Mode: {{dev_mode}}
# Build mode: Local

[workspace]
resolver = "2"
members = ["crates/*"]

[workspace.dependencies]
# IMPORTANT: These versions should match those in the root workspace
bitcode = { version = "0", features = ["derive"] }
bevy_utils_proc_macros = "0.15.0-rc.3"
const_panic = "0.2"
